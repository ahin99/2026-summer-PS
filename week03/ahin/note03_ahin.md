
# 2427

공약수를 구하는 아이디어 : 두 수의 공약수는 반드시 min(a,b) 이하이므로
1부터 반복하면서 두 수를 모두 나눠떨어지게 하는지 확인하면 된다.

이때 입력 범위를 따져서 시간복잡도 고려하기
* 또는, 두 수의 공약수는 두 수의 최대공약수의 약수 gcd(a,b)의 약수와 같음 활용 가능


# 1071 Greatest Common Divisor of Strings

* 유클리드 호제법 = 두 수의 최대공약수(GCD)를 매우 빠르게 구하는 알고리즘.
GCD(a,b) = GCD(b, a%b)  (a>b)

- vector -> vector<string>
- c++에서는 +는 가능하지만 문자열 * 숫자 는 지원하지 않음.
    - for문으로 더해야함

* 리트코드에서 :
- 주어진 클래스 밖에 다른 함수 작성해도 상관없음.
- 또는 주어진 클래스 안에 private함수로 작성해도 됨. (객체지향적으로)

str.size()
str.substr(0, len) -> 시작위치, 잘라낼 길이
a + b
a == b
gcd(a,b)
arr.push_back(1); -> 파이썬 appdend랑 똑같음

1. str2 가 가지는 약수가 뭐가 있는지 확인해야 한다.
-> 있으면 그 약수들이 str1의 약수이기도 한지 확인 (시간복잡도 고려)
-> 약수 없으면 str2가 통째로 str1의 약수인지 확인

2. str2의 약수 확인 방법:

* 생각해 봤어야 할 부분
- b가 a보다 긴 경우 (size는 음수가 되면 엄청나게 큰 양수로 오버플로우 가능)
- 문자열 길이가 정확히 나누어떨어지는가를 봐서 약수인지 아닌지 빠르게 쳐내기 가능
- 최대공약수니까 result의 뒤쪽부터 검사하는 게 더 빠를 거 같다



# 204. Count Primes

벡터에서 특정 원소 삭제하기 :
arr.erase(인덱스)
- 범위로 삭제할 수도 있음

* 에라토스테네스의 체 구현의 아이디어
- vector<bool> isPrime(n, true); -> {true, true, true ... n개 생성}
    - 굳이 1 ~ n 까지의 벡터를 만들 필요 없음. 
    - true -> false로 바꿔주면 됨 (개수 셀때는)
- 0, 1 부터 시작하지 않게 주의 
    - 0은 나눌 수 없고, 1은 모든 수의 약수

for (int i = 2; i * i < n; i++) {
        if (isPrime[i]) {   // i가 소수라면
            for (int j = i * i; j < n; j += i) {
                isPrime[j] = false;
            }
        }
    }

