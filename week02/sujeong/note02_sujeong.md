# 268. Missing Number
- missing number가 중간에 있지 않은 경우도 잘 따져봐야 함. 경우 분할을 잘 해야 한다는 소리.
- 버블 정렬 말고 다른 정렬을 공부할 필요가 있다.

# 349. Intersection of Two Arrays
- python에서 set집합은 array정렬과 다르게 중복을 허용하지 않음.
  - set으로 바꾸면 중복이 모두 사라짐.
  - list -> [], set -> {}
- &으로 교집합을 구할 수 있음. 편리하다.

# 414. Third Maximum Number
- list(set(nums) 이렇게 사용 가능
- set은 순서가 없으므로 index로 접근 불가
- 문제 제대로 읽기
